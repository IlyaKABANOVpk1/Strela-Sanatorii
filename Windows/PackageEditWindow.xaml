<Window x:Class="Strela_Sanatorii.Windows.PackageEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Пакет услуг"
        Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Название и описание -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBox x:Name="txtName"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     materialDesign:HintAssist.Hint="Название пакета *"
                     Margin="0,0,0,10"/>
            <TextBox x:Name="txtDescription"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     materialDesign:HintAssist.Hint="Описание"
                     Margin="0,0,0,10"/>
        </StackPanel>

        <!-- Добавление услуги в пакет -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ComboBox x:Name="cmbService"
                      Grid.Column="0"
                      Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                      materialDesign:HintAssist.Hint="Услуга"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      Margin="0,0,10,0"/>

            <TextBox x:Name="txtQuantity"
                     Grid.Column="1"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     materialDesign:HintAssist.Hint="Кол-во"
                     Text="1"
                     Margin="0,0,10,0"/>

            <Button Grid.Column="2"
                    Content="Добавить"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="AddItem_Click"/>
        </Grid>

        <!-- Список услуг в пакете -->
        <DataGrid x:Name="ItemsGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  Margin="0,0,0,10">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Услуга" Binding="{Binding ServiceName}" Width="*"/>
                <DataGridTextColumn Header="Цена за ед." Binding="{Binding UnitPrice, StringFormat=C}" Width="120"/>
                <DataGridTextColumn Header="Кол-во" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding Total, StringFormat=C}" Width="120"/>
                <DataGridTemplateColumn Header="" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Удалить"
                                    Style="{StaticResource MaterialDesignFlatButton}"
                                    Foreground="Red"
                                    Click="RemoveItem_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Итого и кнопки -->
        <StackPanel Grid.Row="3">
            <TextBlock x:Name="txtTotalPrice"
                       Text="Итого: 0,00 ₽"
                       FontSize="16"
                       FontWeight="Bold"
                       HorizontalAlignment="Right"
                       Margin="0,0,0,10"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена"
                        Margin="0,0,10,0"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Click="Cancel_Click"/>
                <Button Content="Сохранить"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Click="Save_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
