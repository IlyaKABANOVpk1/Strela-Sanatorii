<Window x:Class="Strela_Sanatorii.Windows.GuestSearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Поиск гостя"
        Height="550" Width="700"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Поиск гостя"
                   FontSize="20" FontWeight="Bold"
                   Margin="0,0,0,15" Grid.Row="0"/>

        <!-- Панель поиска -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox x:Name="txtSearch"
                     Width="350"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     materialDesign:HintAssist.Hint="Фамилия, имя, табельный или паспорт"
                     TextChanged="Search_TextChanged"
                     Margin="0,0,10,0"/>

            <ComboBox x:Name="cmbFilter"
                      Width="150"
                      Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                      materialDesign:HintAssist.Hint="Искать по"
                      SelectedIndex="0"
                      Margin="0,0,10,0">
                <ComboBoxItem Content="Всем полям"/>
                <ComboBoxItem Content="Фамилии"/>
                <ComboBoxItem Content="Табельному"/>
                <ComboBoxItem Content="Паспорту"/>
            </ComboBox>

            <Button Content="Очистить"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Click="Clear_Click"/>
        </StackPanel>

        <!-- Результаты -->
        <DataGrid x:Name="ResultsGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  Style="{StaticResource MaterialDesignDataGrid}"
                  MouseDoubleClick="ResultsGrid_MouseDoubleClick">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="150"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="120"/>
                <DataGridTextColumn Header="Отчество" Binding="{Binding MiddleName}" Width="120"/>
                <DataGridTextColumn Header="Табельный №" Binding="{Binding PersonnelNumber}" Width="120"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="130"/>
                <DataGridTextColumn Header="Дата рождения" Binding="{Binding BirthDate, StringFormat=d}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопки -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="Отмена"
                    Margin="0,0,10,0"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Click="Cancel_Click"/>
            <Button Content="Выбрать"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="Select_Click"/>
        </StackPanel>
    </Grid>
</Window>
