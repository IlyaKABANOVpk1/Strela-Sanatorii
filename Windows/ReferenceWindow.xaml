<Window x:Class="Strela_Sanatorii.Windows.ReferenceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Справочники"
        Height="500" Width="700"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Выбор справочника -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
            <ComboBox x:Name="cmbReferenceType"
                      Width="250"
                      Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                      materialDesign:HintAssist.Hint="Выберите справочник"
                      SelectionChanged="ReferenceType_Changed">
                <ComboBoxItem Content="Категории номеров"/>
                <ComboBoxItem Content="Дополнительные услуги"/>
                <ComboBoxItem Content="Роли пользователей"/>
            </ComboBox>
        </StackPanel>

        <!-- Данные справочника -->
        <DataGrid x:Name="ReferenceGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  Style="{StaticResource MaterialDesignDataGrid}"/>
    </Grid>
</Window>
