<Page x:Class="Strela_Sanatorii.Pages.ClientsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Background="{DynamicResource MaterialDesignPaper}">

    <DockPanel Margin="20">

        <!-- Верхняя панель -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,15">

            <TextBox x:Name="txtSearch"
                     Width="250"
                     Margin="0,0,10,0"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     materialDesign:HintAssist.Hint="Поиск (ФИО или табельный)"
                     TextChanged="Search_TextChanged"/>

            <Button Content="Добавить"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="AddClient_Click"/>
        </StackPanel>

        <!-- Таблица -->
        <DataGrid x:Name="ClientsGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  Style="{StaticResource MaterialDesignDataGrid}">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="*"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="150"/>
                <DataGridTextColumn Header="Табельный №" Binding="{Binding TabNumber}" Width="150"/>
            </DataGrid.Columns>

        </DataGrid>

    </DockPanel>
</Page>
